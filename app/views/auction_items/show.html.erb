<% provide(:title, @auction_item.name) %>

<%= image_tag @auction_item.image.url(:medium) %>

<% if Time.now < current_auction.end_time %>
  <%= render 'create_form' %>
<% else %>
  <%= render 'stats' %>
<% end %>

<%= render 'comments' %>
